<!DOCTYPE html>
<html>
<head>
<% include ./partials/_header %>
  <title></title>
</head>
<body>
    <ul>
      <% for (var key in urls) { %>
        <li>
          <form method="POST" action="/urls/<%= key %>/delete">
                <%= urls[key] %> <button>Delete</button>
          </form>
        </li>
      <% } %>
    </ul>
    <a href="">
</body>
</html>